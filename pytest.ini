# pytest.ini
[pytest]
DJANGO_SETTINGS_MODULE = saeedpay.settings
python_files = tests.py test_*.py *_tests.py
addopts = --reuse-db --create-db --tb=short -p no:warnings --nomigrations
markers =
    django_db: mark the test as requiring the database
